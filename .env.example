# Database Configuration
# Currently using SQLite for development
DATABASE_URL="file:./dev.db"

# For Production PostgreSQL (Recommended):
# DATABASE_URL="postgresql://user:password@localhost:5432/netsmartz_db?schema=public"

# For Production MongoDB (Alternative):
# DATABASE_URL="mongodb+srv://user:password@cluster.mongodb.net/netsmartz_db"

# Email Configuration (for contact form)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"
EMAIL_FROM="noreply@netsmartz.com"

# Next.js Configuration
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Optional: Analytics
NEXT_PUBLIC_GA_ID="G-XXXXXXXXXX"
